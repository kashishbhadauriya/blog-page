<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f8f9fa;
    }

    h2 {
      font-weight: 700;
      color: #0d6efd;
    }

    .card {
      border: none;
      border-radius: 15px;
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
    }

    .card-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: #343a40;
    }

    .card-text {
      color: #6c757d;
      font-size: 0.95rem;
    }

    .btn-primary {
      background-color: #0d6efd;
      border: none;
    }

    .btn-primary:hover {
      background-color: #0b5ed7;
    }

    .alert {
      font-weight: 500;
    }
  </style>
</head>
<body>

  <%- include('./partials/nav') %>

  <div class="container mt-5">
    <h2 class="mb-4 text-center">Search Results for "<%= query %>"</h2>

    <% if (blogs.length === 0) { %>
      <div class="alert alert-warning text-center">No blogs found.</div>
    <% } else { %>
      <div class="row">
        <% blogs.forEach(blog => { %>
          <div class="col-md-6 col-lg-4 mb-4">
            <div class="card shadow-sm h-100">
              <% if (blog.coverImageURL) { %>
                <img src="<%= blog.coverImageURL %>" class="card-img-top" alt="Cover Image" style="height: 200px; object-fit: cover;">
              <% } %>
              <div class="card-body d-flex flex-column">
                <h5 class="card-title"><%= blog.title %></h5>
                <p class="card-text">
                  <%= blog.body.length > 100 ? blog.body.slice(0, 100) + '...' : blog.body %>
                </p>
                <div class="mt-auto">
                  <a href="/blog/<%= blog._id %>" class="btn btn-primary btn-sm">Commnet</a>
                  
                </div>

              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
